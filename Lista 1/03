#include <iostream>
using namespace std;

int main()
{
    float A ,B ,C , D;
    int resultado;
    A = 1;
    B = 2;
    C = 0.5;
    resultado = int ((B) * D /C + (A) + C) % int (A + B);

    cout <<"Resultado e:" << resultado;
}
